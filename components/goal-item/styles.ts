import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  goalItem: {
    color: 'white',
    backgroundColor: 'green',
    borderRadius: 6,
    marginBottom: 16,
  },

  itemTextStyle: {
    color: 'white',
    padding: 16,
  },
  itemPressed: {
    opacity: 0.5,
  },
});

export {styles};
