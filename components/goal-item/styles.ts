import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  goalItem: {
    color: 'white',
    backgroundColor: 'green',
    padding: 16,
    borderRadius: 6,
    marginBottom: 16,
  },

  itemTextStyle: {
    color: 'white',
  },
});

export {styles};
